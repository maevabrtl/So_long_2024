#include "mlx.h"

int	main(int ac, char **av)
{
	void	*mlx_ptr;

	mlx_ptr = mlx_init();
	// init mlx
	// get and format map
	// check if valid

	// launch game
	// free and exit properly
	return (0);
}
